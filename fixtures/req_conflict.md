# Conflicting requirement (8.D step 6: conflict surfacing).
# The mock Developer writes the same file for every chunk, so two independent
# chunks that both merge into the primary branch conflict on that file.
c1: Left
c2: Right